import SimplePage from '../components/SimplePage';

export default function PrivacyPage() {
  return <SimplePage title="Privacy Policy" description="We keep account, order, and browsing data limited to what is needed to run and improve the store experience." items={['Secure checkout', 'Limited data use', 'Account controls']} />;
}
