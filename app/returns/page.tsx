import SimplePage from '../components/SimplePage';

export default function ReturnsPage() {
  return <SimplePage title="Returns" description="Return eligible products within 30 days in clean, unworn condition." items={['30-day window', 'Easy labels', 'Refund updates']} />;
}
