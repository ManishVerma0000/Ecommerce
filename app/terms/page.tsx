import SimplePage from '../components/SimplePage';

export default function TermsPage() {
  return <SimplePage title="Terms of Service" description="These terms describe how StepHub orders, returns, product availability, and account use work." items={['Order terms', 'Product availability', 'Customer responsibilities']} />;
}
