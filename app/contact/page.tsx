import SimplePage from '../components/SimplePage';

export default function ContactPage() {
  return <SimplePage title="Contact Us" description="Need help with sizing, an order, or a return? Our support team is ready to help." items={['support@stephub.example', 'Mon-Fri support', 'Order and return help']} />;
}
