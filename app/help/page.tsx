import SimplePage from '../components/SimplePage';

export default function HelpPage() {
  return <SimplePage title="Help Center" description="Quick answers for sizing, shipping, returns, and order updates." items={['Size guide', 'Order tracking', 'Returns and refunds']} />;
}
